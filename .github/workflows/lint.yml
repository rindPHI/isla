name: Lint

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: "3.10"

      - name: Install Python dependencies
        run: pip install black flake8

      - name: Run linters
        uses: wearerequired/lint-action@v2
        with:
          black: true
          black_dir: "src/"
          black_args: '--extend-exclude="(isla/bnf/*|isla/isla_language/*|isla/mexpr_lexer/*|isla/mexpr_parser/*)"'
          flake8: true
          flake8_dir: "src/"
          flake8_args: "--max-complexity=10 --max-line-length=127 --exclude=isla/bnf/,isla/mexpr_parser/,isla/mexpr_lexer/,isla/isla_language/,isla_formalizations/"
